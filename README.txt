If you want to run the project on the Command Prompt, then use these commands:
pip install nltk
pip install keras
pip install tensorflow
python -m nltk.downloader all
python train_chatbot.py
python chatgui.py
